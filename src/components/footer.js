import React from 'react';
import {Layout} from 'antd';
const {Footer} = Layout;

const F = () => {
  return (
    <Footer style={{ textAlign: 'center' }}>
      TronEx Demo
    </Footer>);
};

F.propTypes = {
};

export default F;
